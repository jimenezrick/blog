# TODO: Add command line params and conf file
# TODO: Stdout to syslog?

[Unit]
Description=Blog Server
Wants=network.target
After=network.target

[Service]
Type=simple
Restart=on-abort
ExecStart=/srv/rlog/rlog
User=http

[Install]
WantedBy=multi-user.target
